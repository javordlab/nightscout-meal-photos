# 2026-04-02

## Medication Photo = Confirmation, NOT New Entry (CRITICAL — repeated rule)
When Maria sends a photo of her medication (pill organizer, blister pack, etc.), this is a **confirmation** that the scheduled dose was taken.
- Do NOT create a second Medication entry.
- Update the existing scheduled entry with a confirmation note if needed.
- The `auto_track_meds.js` dedup logic was tightened to enforce strict drug+mealctx matching (any match on same date = skip).
- Duplicate Apr 1 breakfast Metformin removed from health_log.md.

## Charts Broken Since March 31 — Root Cause & Fix
- **Cause:** `chart.mjs` (Vega-Lite renderer) was referenced from a sandbox path that expired after the March 31 session.
  - Stale path: `/Users/javier/.openclaw/sandboxes/agent-health-guard-ce895a4e/skills/chart-image/scripts/chart.mjs`
  - Charts for April 1 and April 2 were the March 31 PNGs re-sent without regeneration (silent failure in `ensureCharts()`).
- **Fix applied:**
  1. Copied chart-image skill + node_modules to stable workspace path: `/Users/javier/.openclaw/workspace/skills/chart-image/scripts/`
  2. Added error propagation in `send_daily_charts_telegram.js` `ensureCharts()` so failures are no longer silently swallowed.
  3. Force-regenerated and resent all 4 charts for April 2 (msg IDs 4289–4292).
- **Permanent fix needed:** Chart scripts must always reference the workspace-stable path, not a sandbox path. If skill is reinstalled, re-copy to workspace.
