- Heartbeat on 2026-04-06 surfaced a persistent repo-cleanliness issue: sync/health jobs keep modifying tracked files (`health_log.md`, multiple `data/*.json/jsonl`, `nightscout-meal-photos/data/*`), so `git status` may stay dirty even after heartbeat sync.
- GitHub push was blocked on 2026-04-06 because backup files under `data/backups/` contained secrets (`auth-profiles.health-guard.20260406-163837.bak`, `auth-profiles.main.20260406-163837.bak`). Future heartbeat/autosync flows should exclude or scrub secret-bearing backup files before commit/push.
