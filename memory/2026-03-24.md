# 2026-03-24 - Tuesday

## Policy Updates
- Enforced hard model lock for image interpretation tasks:
  - Required model: `google-gemini-cli/gemini-3-flash-preview`
  - Non-Gemini models are now explicitly disallowed for image understanding in agent guidance.
- Updated files:
  - `AGENTS.md` (Operational Standards / QUOTAS)
  - `health-guard.md` (Core Directives: model-locked vision + required attribution)
  - `TOOLS.md` (strict logging protocol rules 16-17)
  - `CLAUDE.md` (Agent Operational Rules summary)
- Gateway restarted to apply policy updates.
- Validation run of image tool showed provider-level failure for both configured image paths:
  - `google-gemini-cli/gemini-3-flash-preview` → 403 disabled due to Terms of Service violation
  - `google-antigravity/gemini-3-flash` (added as image fallback) → same disabled state
- Outcome: image model routing is correctly configured, but Google-side service access is currently blocked and must be restored via account appeal/reauth.

## Model Routing Reconfiguration (User-requested)
- Removed model/auth references for:
  - `google-gemini-cli`
  - `google-antigravity`
- Updated OpenClaw model routing:
  - Image model primary -> `openai-codex/gpt-5.3-codex`
  - Image fallback list -> cleared
  - Default model fallback list -> removed `google-antigravity/gemini-3-flash`
- Updated policy docs to match:
  - `AGENTS.md`, `health-guard.md`, `TOOLS.md`, `CLAUDE.md`
- Restarted gateway and validated config load.
- Note: OpenClaw image tool currently reports no registered media-understanding provider for `openai-codex/gpt-5.3-codex`; routing is set, but provider capability for image tool remains unavailable.

## Operational Strategy Shift (User-approved)
- Removed strict image-model enforcement from workspace policy files.
- Switched HealthGuard agent model to `openai-codex/gpt-5.3-codex`.
- Removed dedicated `agents.defaults.imageModel` routing from OpenClaw config (no hard image interpreter route).
- Updated policy to best-effort image understanding in active model context with mandatory model attribution + explicit failure reporting when vision is unavailable.
- Restarted gateway and verified HealthGuard now reports model `openai-codex/gpt-5.3-codex` in agent config.
