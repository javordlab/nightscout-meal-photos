## 2026-03-24
- Root cause of TIR discrepancy (96% vs expected 100%) was stale local cache files (`tmp/glucose_24h.json` and `tmp/glucose_14d.json`) not refreshed before summary scripts.
- Added `scripts/refresh_glucose_data.js` to refresh Nightscout data before computing summaries.
- Updated cron job `30ffd883-4e5c-488c-a242-d3788da0bcef` (`maria-glucose-summary`) to run data refresh + summary/chart scripts and send text plus 4 charts to Telegram group `-5262020908`.
- User preference reinforcement: always include meal predicted peak glucose and predicted peak time in meal logging/reporting outputs.
- New non-negotiable reporting rule from Javi: daily report must always use correct timeframe + exact math (no stale windows, no approximations).
- Report style preference locked in: keep the extended supervisor analysis as one combined casual/friendly grouping (not split into separate “went well/improve/encouragement” subsections).
- Implemented deterministic daily report sender `scripts/health-sync/send_daily_health_report_telegram.js` (09:30 PT via system crontab):
  - Generates report from `scripts/generate_daily_report.js`
  - Validates target date = previous LA day and data point count sanity (fail-closed)
  - Sends report text to Telegram and marks watchdog heartbeat (`primary_deterministic`)
  - Triggers chart sender immediately (same-time delivery intent)
- Added chart fallback at 09:37 PT using idempotent sender (`send_daily_charts_telegram.js --no-regenerate`) to recover only missing images.
- Disabled OpenClaw AI cron job `maria-glucose-summary` to prevent stale/incorrect AI-authored metric drift and duplicate report paths.
