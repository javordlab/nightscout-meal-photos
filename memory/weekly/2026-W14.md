# Weekly Memory Summary — 2026-W14 (Mar 28–Apr 03)

> Coverage: 2026-03-28 through 2026-04-03 | Generated: 2026-04-03

---

## System Changes

### Photo Pipeline — Rebuilt (Apr 2–3)
- **Dead:** `telegram_ingest_updates.js` (getUpdates polling), `photo_to_log_pipeline.js`, `auto_fix_pending_photo_refs.js` — all disabled. Last useful data was March 28.
- **New owner:** HealthGuard uploads photos to freeimage.host in the same webhook turn as logging the food entry. `[photo](URL)` written to health_log.md immediately, no envelope bridge.
- Disabled OpenClaw jobs: `Photo Pipeline - Process Inbound Photos` (1m), `auto-fix-pending-photo-refs` (30m).
- Cleanup pending (48h from Apr 3): delete `photo_to_log_pipeline.js`, `auto_fix_pending_photo_refs.js`, `telegram_media_envelopes.jsonl`, all `pending_photo_entries.json` refs.

### Cron Architecture — System vs. OpenClaw Split (Apr 3)
- Migrated to system crontab (pure scripts, no LLM overhead): `cron_health_watchdog.js`, `daily_log_review.sh`, `audit_health_sync.js`.
- OpenClaw `cron-health-watchdog` job disabled (duplicate of system cron, burned Haiku tokens for nothing).
- System cron watchdog frequency: `*/15` (hourly change `0 * * * *` pending Javi confirmation).
- Provider auth watchdog added: `scripts/health-sync/check_provider_auth.js` — daily 8 AM, checks Nightscout/Notion/Codex/Gemini.

### Claude Code Bridge (Apr 3)
- New service: `scripts/claude-bridge/bridge.js` — Telegram → `claude --print` bridge.
- Bot token in `config.json` (gitignored). launchd plist at `~/Library/LaunchAgents/ai.openclaw.claude-bridge.plist`.
- Auth: Javi (8335333215) and Maria (8738167445) only.

### MySQL Glucose Sync Fix (Apr 3)
- Added 48h age threshold cutoff to `mysql_glucose_sync.js`.
- Root cause: `INSERT IGNORE` silently skips duplicates → loop never detected catch-up → paginated all history every run.

### System Cron PATH Fix (Apr 3)
- Root cause of stale backups.json alerts: scripts calling bare `node` inside `execSync` — system cron PATH has no Homebrew. Silent "command not found".
- Fix: all `execSync` calls patched to use `/opt/homebrew/bin/node`.

### Charts — Stable Path Fix (Apr 2)
- Root cause: `chart.mjs` referenced sandbox path that expired after Mar 31 session.
- Fix: copied chart-image skill to `/Users/javier/.openclaw/workspace/skills/chart-image/scripts/` (stable). Error propagation added to `send_daily_charts_telegram.js` `ensureCharts()`.
- Rule: if chart skill is ever reinstalled, re-copy to workspace path.

### GitHub Pages Architecture (Apr 2)
- Pages moved to `gh-pages` orphan branch (was served from repo root, causing 404s).
- `deploy_gh_pages.js` called after every gallery regen, glucose sync, chart generation.
- Watchdog now monitors `notion_meals.json` age (>2h = alert) and `backups.json` glucose age (>70 min = alert).

### Prediction Model v3 Finalized (Apr 3)
- 4-layer formula: range-based carb factors (2.0/1.3/1.2/0.8) + meal intercepts (Breakfast +31, Lunch −12, Dinner −2, Snack +4, Dessert −14) + cumulative preBG anchor + time-to-peak defaults.
- Expected accuracy: ~87–89% within ±20 mg/dL.
- Written into `AGENTS.md` as canonical.

### OpenClaw Subscription Sunset (Apr 3–4)
- Claude subscription no longer covers OpenClaw as of April 4, 2026 12pm PT.
- Javi obtained Anthropic API key; added to `openclaw.json`.
- Decision pending on API vs. subscription; need 2–3 more weeks of normal-day cost data.

### Cron Schedule Changes (Apr 2)
| Job | Before | After | Reason |
|-----|--------|-------|--------|
| Photo Pipeline | every 1m | DISABLED | Dead pipeline |
| Radial Sync | every 30m | every 1h | Overlap + long run time |
| Weekly Memory Summary | 120s timeout | 300s timeout | Was timing out |
| Glucose Low Alert | 5m, no retry | 5m + 1 retry | Reduce false watchdog alerts |

---

## Rules & Preferences

1. **Medication photo = confirmation, not new entry (P0).** Before writing any med entry, check health_log.md for same drug+date. If exists, skip entirely. Covers all meds: Metformin (all 3 slots), Lisinopril, Rosuvastatin, any future meds. Reinforced multiple times Apr 2.

2. **Photo upload is HealthGuard's job, in the same turn.** No deferral, no envelope bridge, no polling fallback. If Maria sends a photo, HealthGuard uploads it to freeimage.host and writes the URL to health_log.md before replying.

3. **Photo recovery: check inbound folder first.** `/Users/javier/.openclaw/media/inbound/` has all Telegram photos. Never ask Javi to resend.

4. **Cumulative meal rule (Layer 3).** Food within 2h of prior same-type meal = same meal type. Use cumulative carb sum. Anchor preBG to first item's reading. Failure causes ~47–56 mg/dL underestimate.

5. **Chart skill reinstall rule.** Re-copy to workspace stable path if skill is ever reinstalled.

6. **All execSync calls must use full node path.** `/opt/homebrew/bin/node` — never bare `node` in any script called by system cron.

7. **Alert routing (PENDING).** Javi wants all system alerts routed to Claude Code bridge bot, not main OpenClaw bot. Implementation incomplete — see Carry-forwards.

---

## Bugs Fixed

| Bug | Root Cause | Fix |
|-----|-----------|-----|
| Photos missing URLs (Mar 29–Apr 3, 100% failure rate) | `telegram_ingest_updates.js` uses `getUpdates` polling; OpenClaw switched to webhooks late March → no envelopes written → pipeline had no metadata | Moved photo ownership to HealthGuard: uploads in same webhook turn, writes URL to health_log.md immediately |
| backups.json stale alerts (persistent) | Scripts internally called bare `execSync('node ...')` — system cron PATH has no Homebrew → silent "command not found" | Patched all `execSync` calls to use `/opt/homebrew/bin/node` |
| MySQL glucose sync infinite loop | `INSERT IGNORE` skips duplicates silently → loop never detected catch-up → paginated entire history every run | Added 48h age threshold: stop when oldest batch entry >48h old |
| Charts silent failure (Apr 1–2) | `chart.mjs` referenced sandbox path that expired after Mar 31 session; `ensureCharts()` swallowed errors | Copied skill to stable workspace path; added error propagation |
| Duplicate Metformin entries | Medication photo treated as new entry instead of confirmation | Rule: photo of any med = confirmation only; check health_log.md before writing |
| False "outcomes not backfilled" audit alerts (224 false alerts) | Audit checked for non-existent `outcomes_backfilled` flag | Changed to check Notion page_id presence instead |
| Duplicate Notion entries on title edit | Entry key included title → any edit = new key → new Notion page every sync | `radial_dispatcher` now queries Notion by Date+User, not title |
| Duplicate NS treatments every 30 min | Old entries without NS entry_key failed both key-lookup and timestamp-lookup → POSTed as new every run | Added 30-day rolling cutoff in `radial_dispatcher` |
| Duplicate breakfast Apr 2 | Double-logged at 09:30 and 10:09 | 10:09 entry removed from health_log.md, Nightscout, and Notion |
| GitHub Pages 404s | Pages served from repo root; site files at `/nightscout-meal-photos/backup-status.html` | Created `gh-pages` orphan branch; Pages serves from branch root |

---

## Health Patterns

### Glucose
- No CGM data anomalies flagged this week. Prediction model v3 calibrated on n=57 data points (through Apr 2), achieving ~87–89% within ±20 mg/dL.
- Nightscout API auth was intermittently failing (401) on Mar 31 despite correct SHA1 hash — resolved by Apr 2 (no further reports).
- Daily glucose chart generation was silently broken Apr 1–2 (resent Mar 31 PNGs); fixed and regenerated Apr 2.

### Medications
- No medication changes. Rosuvastatin anchor date: 2026-03-01 (every other morning).
- Duplicate medication entries were a recurring problem this week — P0 rule now explicit in `health-guard.md`.

### Diet (Mar 31 sample day)
- Total carbs: 87.5g | Protein: ~76.1g | Calories: ~945 kcal (light day).
- Post-meal walks logged Mar 31: 60 min at 11:01, 30 min at 14:02.
- Portion accuracy issue identified for nuts (cashews): Maria now providing reference photos. ~10 cashews = standard serving.
- Cumulative lunch logged Mar 31: fried rice + cake = 55g total → correctly predicted 180–210 mg/dL peak.

---

## Carry-forwards

1. **Alert routing migration (BLOCKED, incomplete).** Javi requested all system alerts route to Claude Code bridge bot. Needs:
   - Create `scripts/health-sync/alert_config.js` (reads bridge `config.json`, falls back to `openclaw.json`)
   - Update: `cron_health_watchdog.js`, `glucose_low_alert.js`, `check_provider_auth.js`, `audit_telegram_confirmations.js`
   - Commit all changes.

2. **Photo pipeline verification.** New HealthGuard real-time upload flow hasn't been tested with a live Maria photo yet. Monitor next food photo with image.

3. **Cleanup pending (48h from Apr 3, i.e., Apr 5).** Delete: `scripts/photo_to_log_pipeline.js`, `scripts/health-sync/auto_fix_pending_photo_refs.js`, `data/telegram_media_envelopes.jsonl`, all `pending_photo_entries.json` refs.

4. **System cron watchdog frequency.** Pending Javi confirmation: change `*/15` to `0 * * * *` (hourly). No token cost either way; functional downside is minimal.

5. **OpenClaw/Anthropic cost decision.** Normal-day baseline only ~10 days old (biased toward heavy dev days). Need 2–3 more weeks of data before committing to API vs. subscription. OpenClaw sunset was April 4.

6. **Weekly Memory Summary (Codex).** First Sunday run with new 300s timeout — verify it completes successfully.

7. **Claude Code sandbox/crontab visibility.** Under investigation: Claude Code returns 0 cron entries despite 11+ system entries. Workaround: `scripts/dump_system_state.sh` → `data/system_state.md`. Root cause unresolved — may be Claude Code subprocess isolation on macOS.
