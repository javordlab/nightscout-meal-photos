# Memory Log - 2026-03-22

## Permanent guardrails implementation (health sync reliability)

### Delivered controls
- Added hard quality gates in `scripts/health-sync/quality_gates.js`.
  - Blocks placeholder food entries from sync.
  - Requires protein for Breakfast entries.
  - Requires carbs for food entries.
  - Detects when photo URL is missing but matching image exists in internal folder.
- Integrated validation gate into pipeline:
  - `scripts/health-sync/health_sync_pipeline.js` now runs `validate_health_sync` before `unified_sync`.
  - Pipeline aborts on validation errors.
- Upgraded Nightscout sync robustness in `scripts/health-sync/unified_sync.js`:
  - Retry wrapper with backoff.
  - Read-after-write verification.
  - Safer entry-key matching (escaped regex).
  - Stale treatment link recovery path.
- Added report scheduler watchdog:
  - `scripts/health-sync/report_watchdog.js`
  - Emits critical alert if 09:30 report missing by 09:32 PT.
  - Supports fallback command (`REPORT_FALLBACK_CMD`) and heartbeat marking (`--mark-sent`).
- Updated safe sync runner `scripts/run_safe_sync.sh` to include blocking validation.
- Updated `scripts/photo_to_log_pipeline.js` to queue pending-photo metadata instead of writing placeholder entries to `health_log.md`.

### Data corrections applied
- Added missing breakfast protein annotations in `health_log.md`:
  - 2026-03-21 10:03 breakfast now includes `(Protein: 16g)`
  - 2026-03-20 09:04 breakfast now includes `(Protein: 18g)`

### Verification snapshots
- `node scripts/health-sync/validate_health_sync.js --since=$(date -v-2d +%Y-%m-%d)` -> errors=0
- `node scripts/health-sync/health_sync_pipeline.js --mode=sync-only --since=$(date -v-2d +%Y-%m-%d)` -> validate ✓ sync ✓ blocked=0 errors=0
- `node scripts/health-sync/audit_health_sync.js --since=$(date -v-2d +%Y-%m-%d)` -> missingNs=0, missingNotion=0, missingGallery=0, duplicates=0
- `node scripts/health-sync/report_watchdog.js` before heartbeat -> critical missing report alert
- `node scripts/health-sync/report_watchdog.js --mark-sent --source=integration_test` then watchdog check -> OK

## Fixes applied — Round 3 (2026-03-22 evening / 2026-03-23)

### Pipe-split bug in radial_dispatcher (commit `d135004`)
`(Protein: 18g | Carbs: ~45g | Cals: ~340)` format broke fixed-index column parsing. Fixed to dynamic last-two-columns approach. Affected: carbs in Nightscout, photo in Notion, proteins in MySQL, gallery.

### Nightscout timestamp fallback (commit `d135004`)
`find[created_at]=<iso>` always fails on this NS instance. Fixed to `$gte`/`$lte` ±1-minute UTC range query. Also added 30-day rolling cutoff to stop duplicate POSTs for old entries.

### Corrupted dinner treatment
Old pipe-split bug had created a dinner NS entry with `carbs: null`. After pipe-split fix, new entry key differed → entry stayed corrupted. Deleted by `_id`, dispatcher recreated with correct `carbs: 45`.

### check_pending_photos.js removed
Script and cron job removed. Nutrition from vision is mandatory — Maria is never asked for macros. Alert was moot and had a duplicate-send bug.

### HealthGuard persona updated
`health-guard.md` — agent always estimates nutrition from vision. Never asks Maria for macros.

### Gallery now event-driven
Gallery regeneration triggered when photo syncs to Notion (`photoSyncedToNotion` flag), not on hourly cron.

### MySQL proteins column added
`ALTER TABLE maria_health_log ADD COLUMN proteins decimal(5,1) NULL AFTER calories_est`

### Quality gates updated
Protein required for ALL food entries (not just breakfast). Error code: `missing_protein_required_for_food`.

## Fixes applied — 2026-03-23

### Daily report TIR/stats were wrong — rolling count, not calendar day (commit `29262c2`)
`refresh_glucose_data.js` used `count=300` (no date filter) for the "24h" window. At 9:30 AM report time this included prior evening readings, skewing TIR, average, GMI, std dev, CV. Also missing `api-secret` header on all NS requests.
Fixed: exact PDT midnight-to-midnight bounds (`$gte`/`$lte`) for both prev-day and 14-day windows. api-secret added.

### auto_track_meds.js — three bugs fixed (commit `0af111e`)
1. Rosuvastatin cycle: was `dayOfMonth % 2 !== 0` — diverges at month boundaries. Fixed to anchor-date: 2026-03-01 = day 0, take on even days since anchor.
2. Metformin: was entirely unimplemented (TODO comment only). Now logs all 3 doses: 500mg breakfast (≥9 AM), 500mg lunch (≥noon), 1000mg dinner (≥7 PM). Each idempotent.
3. Timezone: date/hour/offset all now use `Intl.DateTimeFormat('America/Los_Angeles')`. Auto-detects PDT/PST.

## Daily report window policy update (requested by Javier)
- New report generator added: `scripts/generate_daily_report.js`.
- Daily report now uses **full previous day** in PT (`00:00-23:59`) instead of rolling “last 24h from runtime”.
- Output written to `data/daily_report_<report-date>.txt` with explicit coverage date in header.
- Metadata written to `data/daily_report_latest.json`.
- Fallback path updated: `scripts/health-sync/send_fallback_report.js` now calls `generate_daily_report.js` before marking report sent.
- Verified by generating `data/daily_report_2026-03-22.txt` with coverage `2026-03-21 00:00-23:59 PT`.

